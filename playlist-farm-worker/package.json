{
  "name": "playlist-farm-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Background worker for syncing playlists from playlist-farm API",
  "scripts": {
    "sync": "tsx sync-playlists.ts",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
